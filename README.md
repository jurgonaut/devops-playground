This repository is a collection of my experiments with `AWS`, `Terraform` and `Ansible`.

Notable projects:
- vagrant-haproxy-laravel-consul: Simulating `Laravel` auto scaling with `Haproxy` and `consul template`
